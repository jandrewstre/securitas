---
- hosts: all
  remote_user: "{{admin_user_username}}"
  sudo: true
  gather_facts: false

  roles:
    - testing
    - common
    - ssh
    - users
    - dns
    - unattended-upgrades
    - monitoring
    # - zerotier
    - ufw
